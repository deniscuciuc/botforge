<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <OutputType>Exe</OutputType>
        <TargetFramework>net10.0</TargetFramework>
        <Nullable>enable</Nullable>
        <ImplicitUsings>enable</ImplicitUsings>
    </PropertyGroup>

    <ItemGroup>
        <PackageReference Include="BotForge.Core" Version="0.1.*"/>
        <PackageReference Include="BotForge.Routing" Version="0.1.*"/>
        <PackageReference Include="BotForge.Messaging" Version="0.1.*"/>
        <PackageReference Include="BotForge.Consumer" Version="0.1.*"/>
        <!--#if (UseObservability) -->
        <PackageReference Include="BotForge.Observability" Version="0.1.*"/>
        <!--#endif -->
        <!--#if (UseTemplates) -->
        <PackageReference Include="BotForge.Templates" Version="0.1.*"/>
        <!--#endif -->
        <!--#if (UsePayments) -->
        <PackageReference Include="BotForge.Payments" Version="0.1.*"/>
        <!--#endif -->
    </ItemGroup>

</Project>
