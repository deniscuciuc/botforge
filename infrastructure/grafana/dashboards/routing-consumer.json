{
  "annotations": { "list": [] },
  "editable": true,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum(rate(teleforge_telegram_routing_updates_total[5m])) by (update_type)",
          "legendFormat": "{{update_type}}",
          "refId": "A"
        }
      ],
      "title": "Updates by Type",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum(rate(teleforge_telegram_consumer_updates_ingested_total[5m])) by (transport)",
          "legendFormat": "{{transport}} ingested",
          "refId": "A"
        },
        {
          "expr": "sum(rate(teleforge_telegram_consumer_updates_processed_total[5m])) by (status)",
          "legendFormat": "processed {{status}}",
          "refId": "B"
        }
      ],
      "title": "Consumer Throughput",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(teleforge_telegram_consumer_updates_queue_lag_bucket[5m])) by (le))",
          "legendFormat": "queue lag p95",
          "refId": "A"
        },
        {
          "expr": "histogram_quantile(0.95, sum(rate(teleforge_telegram_consumer_updates_processing_duration_bucket[5m])) by (le))",
          "legendFormat": "processing p95",
          "refId": "B"
        }
      ],
      "title": "Queue Lag and Processing Latency (ms)",
      "type": "timeseries"
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": {}, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "options": { "legend": { "displayMode": "table", "placement": "bottom" } },
      "targets": [
        {
          "expr": "sum(rate(teleforge_telegram_routing_ratelimit_checks_total{outcome=\"denied\"}[5m])) by (update_kind)",
          "legendFormat": "{{update_kind}} denied",
          "refId": "A"
        }
      ],
      "title": "Routing Rate Limit Denials",
      "type": "timeseries"
    }
  ],
  "refresh": "10s",
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["teleforge", "telegram", "routing", "consumer"],
  "templating": { "list": [] },
  "time": { "from": "now-6h", "to": "now" },
  "title": "TeleForge Routing and Consumer",
  "uid": "teleforge-routing-consumer",
  "version": 1
}
